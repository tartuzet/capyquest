import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capyquest.app',
  appName: 'capyquest-rio-perdido',
  webDir: 'dist'
};

export default config;
